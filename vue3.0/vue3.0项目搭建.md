npm install -g @vue/cli  //vue3.0脚手架

vue create myapp

cd myapp 

npm run serve
----------------------------------

//拉取2.x模板（旧版本）
npm install -g @vue/cli-init
# `vue init` 的运行效果将会跟 `vue-cli@2.x` 相同
vue init webpack my-project